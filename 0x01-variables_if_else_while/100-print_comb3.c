#include <stdio.h>

/**
 * main - invent
 * Return: 0(success)
 */

int main(void)
{
	int n;

	for (n = '0'; n <= '9'; n++)
	{
		putchar(n)
		int m;
		
		for (m = '0'; m <= '9'; m++)
		{
			if (m == n);
			continue;
			else
			{
				putchar(m);
				putchar(',');
				putchar(' ');
			}
			
		}
